package model.enums;

/**
 * Enum representing the status of a resource generator
 */
public enum ResourceGeneratorStatus {
    GENERATING,
    FULL,
    HARVESTABLE // Could combine with FULL or be distinct
}
